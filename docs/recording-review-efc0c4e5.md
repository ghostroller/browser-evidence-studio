# 京东示范存档检查：efc0c4e5

检查日期：2026-09-23。目标 run：`efc0c4e5-e9b0-470a-8f0b-0ec5636f1c4f`，北京时间 15:25:58–15:40:27，已封存。本轮只读存档与代码，未操作站点、重新登录、重建索引或开始编写业务 agent。

## 判断

**材料足以开始主流程初版编写；连续采集有明确缺口，全部业务要求的验收基线仍需补充。** 登录、个人信息、实名认证、地址、普通订单筛选和翻页、空回收站、单笔订单详情都有保存点。主要待补范围是末页停止、非空回收站、其他年份/详情变体，以及身份字段与遮罩值的输出约定。本轮遵照用户要求停留在评估。

## 保存完整性与采集连续性

两者分别核验，不能用封存成功替代采集完整性：

| 检查项 | 实际结果 |
| --- | --- |
| 保存记录 | 8,739 events、4,230 artifacts、14 checkpoints、4,865 raw，共 17,848 条；合并后的 sequence 1–17,848 连续，无重号或漏号 |
| 文件、索引与引用 | 44 份源 JSONL 有效且尾行完整；索引 offset/bytes 匹配；11,158 次 artifact 引用均有效 |
| 原件校验 | 947 条带文件的 artifact 对应 699 个唯一 blob，大小及 SHA-256 全部通过；封存清单中的 743 个文件全部通过 |
| 保存点 | 14/14 截图和 DOM 均 complete，captureOutcome=completed、captureConsistency=consistent；这表示采集时间段内身份一致，不表示页面被冻结 |
| rrweb | 2,556 条，其中 50 个全量、2,442 个增量；13 个 page/generation/frame 分组都有全量基线，未见增量早于该组首个全量；4 个初始全量前未见对应 Meta，不宣称逐帧完整回放 |
| 采集缺口 | 68 个 gap；其中 3 个 capture-backpressure 汇总共 **5,939 个采集任务被丢弃**，另有弹窗接入盲区、请求关联缺失、关闭时在途请求及 WebSocket 边界 |
| 响应正文 | 703 complete、15 empty、107 read-failed、16 missing、1,052 excluded；二进制主动排除与读取失败分开，不将 excluded 全算成异常 |
| 请求正文 | 201 complete、2,067 not-applicable、40 excluded、1 read-failed |

背压分组计数为 4,729、903、307，证据 `evt-000000017834`、`evt-000000017847`、`evt-000000017848`。它们在封存 flush 时汇总，无法据该落盘时间定位实际缺失时段，也不能把任务数当成丢失事件条数或推算丢失比例。编号在持久化时分配，队列丢弃发生在此前，因此已保存编号连续与现场采集缺失可以同时成立。

其余 gap：27 个 response-without-observed-request、27 个 completion-without-observed-request、3 个 popup-before-capture-ready、6 个 capture-stopped-in-flight、1 个 request-body-read-failed、1 个 WebSocket 能力边界。107 个响应正文读取失败均记录 `Network.getResponseBody: No resource with given identifier found`；读取失败不等同于站点请求失败。

## 业务线索覆盖

| 环节 | 证据与实际观察 | 对后续编写的意义或限制 |
| --- | --- | --- |
| 扫码登录 | `cp-000000003013` → `003170` → `004612`，对应登录页、扫码待确认、登录后首页 | 可确定人工登录等待与成功后的页面阶段；没有扫码超时、过期或再次登录的分支样本 |
| 个人信息 | `cp-000000005932`；DOM `art-000000005931`，原始 HTML 响应 `art-000000005502` | 页面可见账号名、昵称，当前性别为保密、生日未选择。checkpoint DOM 删除 input 的 value，不能把该空属性当作真实空值；原始 HTML 保留昵称。独立用户 ID、登录名与账号名的语义映射，以及个人邮箱来源尚未确认 |
| 实名认证 | `cp-000000006381`，DOM `art-000000006380` | 保存了已认证页面；姓名、绑定手机由站点遮罩。现有样本仅能支撑按显示值采集，不能承诺还原完整姓名/手机号 |
| 地址入口及列表 | `cp-000000008504` → `008855`，动作 `evt-000000008533`；列表 DOM `art-000000008854` | 地址管理入口、打开新页和列表衔接可追溯；有 8 条地址、1 条默认。收货人/地区/地址有值，8 条手机均遮罩，固定电话和电子邮箱为空；结构足以编写列表提取，空值与遮罩值需分别保存 |
| 普通订单及筛选 | `cp-000000010292` → `011590` | 近三个月样本 8 条，切换今年内后的第 1 页为 20 条；支持筛选前后状态变化，不能据此声明所有年份已遍历 |
| 普通订单分页 | `cp-000000013242`、`013336`；响应 `art-000000011021`、`012641` | 今年内第 1、2 页各 20 条，orderId 交集为 0，响应均报告 totalNum=54；页面显示共 3 页。两保存点 DOM hash 相同，是同一第 2 页状态的补充说明；尚无第 3 页/末页停止样本 |
| 已删除订单 | `cp-000000014221`，DOM `art-000000014220` | 回收站实际为空，保留年份选项；支持入口和空列表判断，不能据此确认非空回收站的字段、分页及详情行为 |
| 订单详情与结束 | `cp-000000017711`、`017830`，DOM `art-000000017709`、`017829` | 详情实体匹配第 2 页中的一个订单，收货、配送、付款和金额字段有内容，手机号遮罩。两保存点截图和 DOM hash 均相同，记录的是同一详情状态。标题中的“每笔”“完成”是演示意图，不是已遍历全部实体的证据 |

个人信息和实名认证的截图已实际查看；表中其余结构与数量由已保存 DOM、动作和元数据核对。未将页面上的账号、订单号、姓名、地址或手机号复制到本文。

订单网络线索可支持后续选择数据来源：`pcorder_center_list` 请求正文 `art-000000009215` 为 `d=1,page=1`，`010865` 为 `d=2,page=1`，`012436` 为 `d=2,page=2`，共同的 `s=4096`。请求与相应 JSON 正文完整；响应包含 `orderId`、`parentId`、`dateSubmit`、`itemPayInfo`、`consigneeInfo`、`statusInfo`、`shopInfo`、`wareList`。两页分别有 1、6 条 `parentOrderList`，已出现父子订单结构，后续去重、详情遍历与覆盖统计必须明确实体层级，不能简单按 DOM 卡片数量等同订单总数。其他年份的取值不能仅由当前两次请求推断。

## 后续编写与验收需补充的范围

1. 明确用户 ID、登录名、账号名的对应关系和个人邮箱来源；允许未设置、不可获取、站点遮罩分别表达，不能统一成空字符串或从遮罩推断原值。
2. 补一组同一筛选条件下的末页/停止样本；跨年份至少核验一个历史年份及无订单分支。现有第 1→2 页证据可用于初版设计，不能代替终止条件验证。
3. 如果交付要求覆盖非空回收站，需要非空样本；没有样本时明确列为待验证范围，不为了补录去删除真实订单。
4. 详情只提供一个状态样本；后续按列表实体主键关联详情并核验，父子订单的详情策略及其他状态、拆单/多商品等变体不能从当前样本外推。
5. 为关键阶段定义稳定 checkpointKey、requirementId 与输出字段规则。当前 14 个保存点的 requirementIds 均为空、key 为自动生成值；可以据现有描述整理契约，不把标题直接作为机器断言。

已有线索无需整段重录。补录应集中在上述缺口；连续采集背压则是客户端自身的独立问题，需后续修复和合成验证，不能由业务代码掩盖。

## 定位器与环境补充

记录了 13 个 action 和 4 个 element-selected，候选 selectors 为空；文本、DOM 结构、页面身份和导航仍可用于后续确定定位器。地址页、回收站和详情保存点中存在沿用上一页面/导航代际的 selection 元数据，不能将它当作当前页面的定位对象；应以保存点自身 pageId/generation 及 DOM 为准。

本次使用 profile `5f7b9163-109a-4e36-aa3c-0e99f0f4b2bc`，与此前异常 profile 不同，仍在同一 Forge 开发实例 PID 35448。已保存的 1,976 个 network-response 为 1,943 个 200、2 个 206、31 个 304，未观察到 403。该结果支持“同一开发实例的新 profile 可以完成本次主线”，进一步缩小旧登录环境的排查范围；存在采集缺口，不能声称全部网络请求均正常或已经证明旧 profile 的具体失效原因。

本轮 Node 24.21.0 / npm 11.19.0；通过本机只读 HTTP 有界分页取得摘要、缺口和选定元数据，再按 artifact ID 定向检查保存的原件。文件/索引/hash 独立核验不改原存档。本地诊断脚本及报告位于 Git 忽略的 `output/archive-audit-efc0c4e5/`，包括 `integrity-report.json`、`fields-summary.json`、`actions-summary.json`、`orders-coverage.md`；未运行产品回归或业务脚本。
