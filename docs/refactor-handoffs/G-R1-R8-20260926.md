# R1–R8 限界修复交接（2026-09-26）

## 边界与保存

集成点为 `D:\Workspace\browser-evidence-studio` 的 `main`，起点 `f55e015`。只收拢本批所需 A `550f30a → 2a6b666`、D `7b772bf → 4fb108d`、E `83aa396 → 850a306`，保留这批 `output/refactor-worktrees` 下的原工作树/分支及既有 B/C/F 成果，不重置或重复搬运。R1–R8 的后续实现与回归提交为 `f62e5f4`。用户已有未跟踪的 `docs/refactor/04-review-and-execution-correction.md` 和 `docs/refactor/review-evidence/` 保留原状，不纳入本次提交。合成录制、profile、失败输出和旧日志均留在忽略的 `output/`，不入 Git。

本批按修正后的执行约定工作：主集成者处理 Git、验证和收敛；一个额外 Sol/high 实施流仅处理授权与资料 UI 的独立文件，一个 Astra/high reviewer 只读复核源身份、连续播放、资源隔离和选择回执。两者模型/强度由原生 Agent 创建参数指定，没有递归派发或重做完整技术审查。

## 实现与回归

| 项 | 已完成的限定范围 | 复现与证据 |
| --- | --- | --- |
| R1 | 同批 remove/add 移动继续保留原节点及后代的源元数据；源模型和存档节点仍定位 `#source-b` 中的 `data-original=stable-source`。 | 真 rrweb DOM 移动单测、系统桌面 `movedSource`。 |
| R2 | ReplayHost 用 rrweb 原生 Replayer 连续播放，暂停/时钟由宿主状态对齐；跨 full snapshot 精确重建并继续，迟到 finish 不覆写新代际。 | React 播放边界测试；真实 UI 241 事件播放、暂停、前后 seek。 |
| R3 | 在每个源事件/逻辑 frame 查找资源；CSSOM/adopted style 与 frame 的 CSS/图片进入生产离线回放；只对可加载属性报告缺失，普通导航链接不再误报；保留真实缺失诊断，不放松 CSP、sandbox 和脚本阻断。 | 资源单测及关闭源站后的新 Electron 进程生产 ReplayHost，资源 `ready` / blocked 0。 |
| R4 | 字段/注释选择带项目、草稿版本、卡片、目的和回放代际回执，迟到或不同选择不串绑。 | renderer 回归及真实原生鼠标/Escape/蒙版/UI 流。 |
| R5 | state、jobs、取消、幂等与资料/结果读取均按任务授权范围及撤销检查。 | 定向权限回归；系统链真实授权后执行、撤销后 HTTP 403。 |
| R6 | 移除旧“一次授权”公开入口及其 UI/技能/API 描述，统一当前任务授权。 | API/权限测试与文档对齐。 |
| R7 | 卡片中创建并关联需求，注释新增/修改/重绑/删除；固定版本可查看并派生草稿。 | React 测试及真实 UI 卡片→字段→需求→注释修改→发布固定版本→派生。 |
| R8 | 逐数据集隔离坏 manifest/state/未初始化目录；崩溃后好批次保持可读。已完成执行只读审计额外目录及固定清单中的损坏，额外目录只给诊断、不冒充可选数据集。 | 强杀宿主后重开单测、React 结果测试、真实 UI 中坏目录与好数据并存。 |

真实命令和结果：Node `v24.21.0` / npm `11.19.0`；`npm test` **50 文件 / 298 项通过**，`npm run typecheck`、`npm run build` 通过。最终日志分别在 `output/full-unit-refactor-20260926-final.log`、`output/typecheck-refactor-20260926-final.log`、`output/build-refactor-20260926-final.log`。定向资源测试 19/19，坏目录强杀恢复测试 1/1。

桌面测试串行执行：`node test/desktop/launch.js --refactor-recording` 的 record/offline **2/2** 通过，`output/desktop-1790384001917/refactor-recording-summary.json`；生产离线 ReplayHost 的同源 frame 样式/图片、CSSOM/adopted style、历史资源版本及脚本阻断通过。`node test/desktop/launch.js --refactor-system` **通过**，`output/desktop-1790384521194/refactor-system-report.json`；源站关闭后 React UI 完成 §10 的录制、回放、源节点、资料编辑、授权执行和结果查看。好值 pass、错误值 fail、证据 partial 仍有 2 条提交记录；人为放入损坏目录后 UI 有 `INDEX_RECOVERY_REQUIRED`，好数据可打开，新增目录不可选。初次受限 Electron 的 GPU 启动失败、链接资源误报、坏目录断言和 UI fixture 定位失败的原始日志均保留；最后两份桌面报告只陈述最终通过范围。

## 未完成的整体目标

本批只验证合成页面和同源 frame 的指定离线资源与操作路径。跨源/其他资源边界、长档性能、完整旧桌面矩阵、发行安装包、新架构 30 分钟长测、交接导出/技能独立接续、真实账号与真人协作仍待后续验收。录制成功不等于这些需求已通过；源数据仍来自原始录制与耐久采样，不从回放 DOM 补造。

## 保存记录

实现与测试为 `f62e5f4`，先前 A/D/E 映射如上；本文件及进度/验证/状态文档作为后续文档提交保存。工作树、录制原件、失败日志与未跟踪审查文档不清理。
