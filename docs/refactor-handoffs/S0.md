# S0 交接（2026-09-26）

范围：T00/T01 的共享边界和最小真实闭环，不实施 A/B/C 全包。集成目录固定 `D:\Workspace\browser-evidence-studio` 的 main。输入规范原样提交，未 reset/stash 未提交内容，未删除任何录制/profile/工作树，不推送远端。

## 提交与代码

起点 `8175d4b4d8fefeb37c463571f8cc4fa7ae689429`；共享类型与契约提交 `2e99556`。session `1ec1341`、串行 teardown `baf5f14`、模式场景 `a4943d1`、UI/模式接入 `c83c74c`；rrweb 原型 `92c3aaa`、隐私策略/测量 `d0fb22c`；关闭确认及修正为后续 fix(session) 提交。完整图以 `git log 8175d4b..HEAD` 为准，不 cherry-pick 未确认工作树 HEAD。

A/B/C 从最终 `codex/s0-ready-20260926` 这一固定基线分支建立；在任一新工作树运行 `git rev-parse codex/s0-ready-20260926` 得到真实完整 base SHA。S0 准备完成后该基线分支不再移动。工作包依赖契约版本 v1，不只依赖聊天记忆。

## 实际新工作树

所有目录本次新建，位于忽略的 output 内，不进入包/Git；不复用旧 Codex worktree。保留两份 S0 实施树供审计，不把它们当 A/B/C 的基线。

| 包 | 实际目录 | 分支 | 数据根（绝对） |
|---|---|---|---|
| A | `D:\Workspace\browser-evidence-studio\output\refactor-worktrees\20260926-A` | `codex/refactor-a-20260926` | 同目录 `output\data-A` |
| B | `D:\Workspace\browser-evidence-studio\output\refactor-worktrees\20260926-B` | `codex/refactor-b-20260926` | 同目录 `output\data-B` |
| C | `D:\Workspace\browser-evidence-studio\output\refactor-worktrees\20260926-C` | `codex/refactor-c-20260926` | 同目录 `output\data-C` |
| S0 session | `D:\Workspace\browser-evidence-studio\output\refactor-worktrees\20260926-s0-session` | `codex/s0-session-20260926` | 不运行 Electron，由集成 owner 验证 |
| S0 replay | `D:\Workspace\browser-evidence-studio\output\refactor-worktrees\20260926-s0-replay-prototype` | `codex/s0-replay-prototype-20260926` | 不运行 Electron，由集成 owner 验证 |

每流新建自己的 node_modules（`npm.cmd ci`，需正常依赖缓存/网络；不要链接主树可写依赖缓存），核对实际 Node/npm 后启动。PowerShell 示例，A/B/C 替换对应字母：

```powershell
Set-Location -LiteralPath 'D:\Workspace\browser-evidence-studio\output\refactor-worktrees\20260926-A'
node --version
npm.cmd --version
git status --short
git rev-parse HEAD
$env:BES_DATA = Join-Path (Get-Location) 'output\data-A'
npm.cmd ci
npm.cmd run typecheck
npm.cmd test
# 与集成 owner 协调后才启动可见桌面：
npm.cmd run start:agent
```

Node/npm 仍为 24.21.0/11.19.0；没有升级依赖。测试启动器自行新建本工作树 output/desktop-<timestamp> 并设置 BES_TEST/BES_DATA；`start:agent` 使用上面各流的独立 BES_DATA。连接文件位于该根的 `connection/agent-connection.json`，开发摘要位于本工作树 `output/dev/latest.json`；检查 instance/project/profile，不读取别流 latest。HTTP/CDP/Vite 沿用动态端口 0，无固定端口冲突，也不公开内部 CDP endpoint。桌面/长测仅一个 owner 运行。

## 启动提示

A：

```text
执行工作包 A（T02–T05）。先读 AGENTS.md、docs/refactor/01-modification-plan.md 第3、5–8、16–17节，docs/refactor-contracts.md 和 docs/refactor-handoffs/S0.md。核对当前分支基线 codex/s0-ready-20260926。只拥有 capture/evidence/replay/resources 及相关测试，不能私改共享契约、主 UI、package/lock。先接入 S0 真实源信息与隐私风险发现：rrweb checkbox value 在 maskAllInputs 下仍可能泄漏，原型采集时策略已验证但生产未接入；同毫秒 eventSeq、frame/plugin ID 变换尚未证明。完成连续采集/分段/分类缺口、离线资源、有界 replay 和原始定位器，不用回放 DOM 自证原站定位器。实际测试、按工作包提交，独写 docs/refactor-handoffs/A.md，列接口变化提案、证据、限制和接续点。
```

B：

```text
执行工作包 B（T06）。先读 AGENTS.md、主规范第3、9、16–17节、docs/refactor-contracts.md 和 S0 交接，核对基线 codex/s0-ready-20260926。只拥有 src/materials 及相关测试，使用 src/contracts/materials.ts，不改 renderer、原件 writer、根配置或共享契约。实现项目级要求/字段、多示范 checkpoint 关联、并发条件草稿、不可变资料 revision/hash、差异和旧档只读投影。复制不改原件，移动后复核绑定，V1不随V2改变，发布不等于人工批准。运行真实模块/适配测试，按包提交，独写 docs/refactor-handoffs/B.md；无真实 A 接入时明确标待集成。
```

C：

```text
执行工作包 C（T09/T10）。先读 AGENTS.md、主规范第4、10–11、16–17节、docs/refactor-contracts.md 和 S0 交接，核对基线 codex/s0-ready-20260926。只拥有 runner、便携 helper、归属样例和相关测试，使用 execution.ts/旧 WorkflowReporter 适配，不私改共享契约、main/dispatch或最终 UI。保留普通 Puppeteer；实现步骤依赖/独立失败、真实取消静默、有限幂等重试、新 attempt 和批次耐久输出。当前页试跑/从起点模式已有 S0 接入，资料 revision 绑定需等 B 接入，不能伪造已完成。测试强杀后部分数据、批次幂等、原始错误和取消后无迟到动作，提交并独写 docs/refactor-handoffs/C.md，不造 DSL。
```

## 真实验证与限制

完整原始日志在主目录 `output/refactor-s0-20260926`，起点失败见 refactor-baseline.md。测试代码基线为 `cee34da`，其后只有 popup 失败的只读诊断和文档更新；不能把子代理模块测试当端到端。

| 实际命令 | 结果 / 产物 |
|---|---|
| `npm.cmd run typecheck` | 通过；final-typecheck.log |
| `npm.cmd test` | 28 文件/161 项通过，55.28 s；final-unit-161.log |
| `npm.cmd test -- test/unit/api-boundary.test.ts test/unit/validation-recovery.test.ts` | 19 项通过；final-affected-test.log |
| `npm.cmd run test:integration -- --refactor-s0` | 全部 S0 闭环通过；s0-integrated-sixth.log；`output/desktop-1790364663793/refactor-s0-summary.json` |
| `npm.cmd run test:desktop`（中间集成版本） | 完整 19 进程矩阵通过；integrated-desktop-2.log；`output/desktop-1790364354654/desktop-summary.json` |
| `npm.cmd run test:desktop`（新默认执行模式） | main 走到 checkpoint popup 首次点击失败，保留原错；final-desktop.log；`output/desktop-1790364750676`；后续增加只读诊断，未改点击/超时/断言 |
| `node test/desktop/launch.js --recovery-only`（最终执行/恢复语义） | 5 处强杀×重开×重复重开及 2 处退出确认，17 进程通过；final-recovery.log；`output/desktop-1790364881384/desktop-summary.json` |
| `npm.cmd run test:desktop`（最终代码 + 只读失败诊断） | **完整 19 进程矩阵通过**；final-desktop-diagnostics.log；`output/desktop-1790364974479/desktop-summary.json`。未跳过 UI，popup 点击原断言通过，profile 新进程、5 处强杀/两次重开和 2 处退出重入均通过 |

真实 S0 session 证明：同一 target/webContentsId/pageId/generation 保留未提交输入和 JS；停止检查监听后原生合成输入仍可用；第二段新 writer 和 full snapshot 包含停录期间状态；原始 17 个文件 hash 不变；故意 teardown 失败不报 sealed，重试成功；beforeunload 两次真实 CDP/原生拒绝（各 2 个事件）保留页面，移除 fixture handler 后等待实际 destroyed。执行模式使用普通 worker 读取随机 JS marker/表单/cookie，当前页保留，from-start 新页从显式 URL 开始但共享 profile；默认模式也新建，空 session 可重建，模式保存到 manifest/catalog。

S0 多次初始失败均保留：测试入口缺白名单、原型发现 stock rrweb 输入泄漏、过早关闭返回、beforeunload 的 CDP/原生通知竞争、测试读取销毁后 view getter 等。修复针对源码/真实观察，没有删隐私 sentinel、放宽关闭拒绝/销毁断言、忽略错误或改历史数据。原始基线的深色存档恢复失败、后续 popup 首次点击失败仍作为间歇风险记录；偶次通过不宣称其根因已修复。原生窗口整合截图曾超时，既有测试记录为分别保存 renderer/native surface，不冒充整窗截图；OS 物理鼠标路径未测。

硬件（Node os 实测）：Windows 10.0.26200 x64，i7-11700K @3.60 GHz，16 logical CPU，34185228288 B RAM。使用同机 Node os 读取，WMI 在受限账户拒绝访问。没有同跑其他 Electron 测试；单元测试可能并行，占用另计。

已通过 rrweb 真实专项：`npm.cmd run test:integration -- --refactor-replay`，`output/desktop-1790364158993`。6 个实际事件，11826 B，27 ms 源时间跨度；初始化 33.57 ms，6 次 seek P50 2.10 ms/P95 5.17 ms。窗口隐藏、backgroundThrottling=false；样本很短，不代表生产吞吐或增长。报告分主/源/replay 进程内存，不能据首尾快照断言无泄漏。源码 hooks 只在此原型使用，未修改 node_modules。

合并 S0 完整专项的另一份真实样本：6 事件、11826 B、26 ms 源跨度，seek P50 1.52 ms/P95 27.88 ms。两个样本均报告，不挑较快值替代全部结果。队列/磁盘吞吐和 replay worker 增长尚未由新 A 流水线测得，沿用旧固定负载门槛并明确待测，不虚构性能通过。

rrweb 2.1.6 UMD SHA256 `a95ac410da31a007385cc6df5ae2446af4cb5eff397d3cc6fe0574c3bee957e4`。源码证据：mirror.add 后 onSerialize；record options 未公开此 hook；attributeMap 前能读 raw attribute；跨 frame Plugin payload 原样通过，Custom 只改固定字段；公开 pause 按 timestamp 严格小于边界，eventSeq 精确 seek 待 A。控件隐私缺口在采集时 eventProcessor 中修正，保留 checked/selected；不修改历史数据。

尚未宣称完成：生产源属性/统一隐私策略、跨 frame/shadow/Canvas、离线资源、同毫秒精确 seek、新录制格式 2、长历史与多段 isolated world 内存增长、资料领域、模块 runner、固定资料验收、安装包、真实账号、物理鼠标和新 Agent 端到端。旧生产录制 schema 1 与已有业务示例仍在，不迁移真实原件。

## 最后接续

S0 的源代码、契约和上述测试闭环完成。A/B/C 目录已准备但未启动实施会话，也未为这三个目录安装依赖；先按上面的 `npm.cmd ci` 初始化各自 node_modules，不借用主目录缓存/真实 profile。S0 留存分支不继续开发，后续只从冻结基线启动。完整矩阵的一次通过不证明间歇 UI 问题根因已消除，A/D/G 保留本轮诊断继续复核；不重复全仓升级或批量格式化。
