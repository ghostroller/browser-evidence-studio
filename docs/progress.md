# 当前进度与接续

更新日期：2026-09-23。当前为已有合成通过记录的开发版本，M0–M5 核心已实现，异常 writer 与验收恢复已完成本批收尾；长历史、示范基线等仍有缺口，M6 尚未整体验收。本文用于接续工作，历史命令和本次审查边界详见 [verification.md](verification.md)。不能把“核心已实现”理解为各阶段全部验收目标完成。

最新前端重构已按“先文档后实现”完成：shadcn 组件、亮/暗主题、紧凑左右布局和可调整信息区已通过类型检查、102/102 测试、19 进程桌面矩阵及 Forge 开发验证，报告 `output/desktop-1790104067949/desktop-summary.json`。同步修复窗口销毁后的迟到 IPC 异常与被拒导航导致浏览器隐藏的问题；Windows 物理鼠标跨原生视图命中仍待人工体验检查。详细范围见 [前端方案](frontend-refactor.md) 与 [验证记录](verification.md#前端重构与窗口-ipc-修复2026-09-23)。下方较早批次的计数和锁文件说明保留历史口径。

前端交接后已完成统一源码别名：`@/* -> ./src/*`，三份 Vite 配置读取 TS paths，shadcn 五个 aliases 指向 `@/renderer/...`。隔离 CLI 实际生成、类型检查、102/102 单测、19 进程矩阵、Forge 开发/HMR、package 和新包主阶段通过；首轮桌面拖动曾被额外 `buttons=0` 事件打断，同构建未改代码重跑通过，事件来源仍未确认。CLI 直接联网问题也未宣布修复。详情见 [路径方案](import-alias-plan.md) 与 [验证记录](verification.md#统一源码别名2026-09-23)。本次没有迁移测试框架。

## 工作目录与提交

- 固定开发目录：`D:\workspace\browser-evidence-studio`，分支 `main`。后续代码、测试和文档均在这里维护。
- 实现提交：`f8e6eb8`，包含 Vite 客户端、采集与证据、HTTP、执行/验收、示例、测试及最后的 reader 预算修复；从 `codex/initial-client` 快进合并，无冲突。
- 旧 worktree：`C:\Users\Administrator\.codex\worktrees\c22c\browser-evidence-studio`。停止在此开发，保留迁移前本地证据；原长测进程已退出，不再在旧目录启动测试。Git 合并只搬运跟踪文件，不复制录制、Cookie、profile 或其他运行数据。
- 历史文档提交为 `1c3c3b1`，审查与通用 Node 环境说明为 `14f64fb`；请求正文/checkpoint/评审为 `10f8f5d`，writer 锁为 `7a6ef3f`，验收持久化与恢复界面为 `9946a4b`。后续退出诊断与矩阵验证见本批提交记录。不要将开发验证结果视为正式签名发布或真实业务验收。

## 阶段进度

| 阶段 | 已实现与已验证 | 仍需完成或确认 |
| --- | --- | --- |
| M0 桌面技术闭环 | Electron Forge + Vite + TypeScript + React；WebContentsView、明确 targetId、独立采集/操作连接、原生蒙版；开发启动、真实 Electron 和打包 EXE 已运行 | 每次最终源码改动后保持产物与验证版本一致 |
| M1 证据与恢复 | 追加原件、JSONL/可重建索引、hash、尾部损坏恢复；请求/响应正文独立状态与预算；已补 OS writer 身份、独占回收、原锁保留、原子发布和重复重开幂等 | 采集/索引进程分工；长负载及数据损失结论仅限实际测试边界，非 NTFS/其他桌面平台未验收 |
| M2 录制工作台 | 项目/profile、录制、checkpoint 截图/DOM、元素检查、时间线、历史证据和顶层 DOM 回放；已补 10 秒采集期限、定向取消、部分材料保存及晚到结果隔离 | 长历史续读与回放定位、基础导航与缩略图仍有缺口；完整跨域 iframe/Canvas/媒体回放不作承诺 |
| M3 人机协作 | 可撤销操作连接、控制权/代际、人工完成检查、取消与超时；两处人工窗口、合成登录态跨进程复用和 profile 隔离；等待人工时强杀后保留材料并转中断 | 真实扫码、真实会话失效及真人恢复体验；不承诺恢复旧执行栈和人工等待 |
| M4 本机 HTTP | loopback/token/Host/Origin、异步 job、幂等、身份验证、有界证据读取；真实 8 个完成 job、6 次拒绝、幂等点击仅一次 | 长负载下 HTTP 性能目标与来源级技能接续 |
| M5 普通脚本与验收 | 普通 Puppeteer worker、薄 manifest、字段/覆盖/来源/版本验收；评审回读与同项目示范过滤；已补启动登记、报告/终态提交、证据校验重建和中断后新 run 重试 | 示范基线持久关联，以及真实业务需求与新脚本的端到端验收；独立 Edge 五变体仍为历史通过记录 |
| M6 交付 | 项目技能、统一合成夹具、20 分钟测试入口；实例/PID/清理阶段退出诊断；最新前端与别名源码已 package，新包主阶段通过；历史 ZIP 与旧包两进程回归通过 | 用新诊断复验长测、技能独立接续、新发行 ZIP 与真实业务验收 |

实际源码主要位于 `src/main/services/studio.ts`、`src/main/services/dispatch.ts`、`src/capture/`、`src/evidence/`、`src/runner/` 和 `src/renderer/app.tsx`；架构/实施文档中的部分细分目录仍属于职责规划。

## 本次代码与工作区核查

初次静态审查基线为 `main` / `1c3c3b1`（实现 `f8e6eb8`），审查时未重跑测试；其文档结果已提交为 `14f64fb`。用户随后授权实施下一步，本轮恢复依赖并补齐以下数据、控制与评审缺口。实际运行记录见 [verification.md](verification.md)，未访问真实账号材料。

本轮终端实际 Node/npm 为 **24.21.0 / 11.19.0**；已重新安装依赖、构建客户端，并产生新的合成测试证据。项目仅约束 Node/npm 版本，安装方式和版本管理器由开发者选择。锁文件内容未改；没有重新生成发行 ZIP，历史产物和旧报告不改写为本轮通过。

上一批 `10f8f5d` 的 **65/65 单测**、Vite 构建与两进程 Electron 报告为 `output/desktop-1790092245546/desktop-summary.json`。当前批最终类型检查、**94/94 测试**、Vite 构建和 **19 进程 Electron 回归**均通过，最终报告为 `output/desktop-1790095701394/desktop-summary.json`。覆盖原有主场景/profile 重启、五个强杀位置的两次重开与新 run 复跑，以及目录保存期间保持 finalizing、已有 checkpoint 下连续关窗/app.quit 的完整清理。详情见 [verification.md](verification.md)。

初次审查项及本轮收尾后的边界：

| 项目 | 源码依据与当前边界 |
| --- | --- |
| 长历史浏览与回放 | 前端重构移除了列表前 15 个 run 的额外裁剪，现显示当前加载范围；state 的 run 上限与 checkpoint/gap 续页仍未补齐。`Studio.history()` 首批 checkpoint 最多 100 条且受 32 KiB 限制。`Studio.replay()` 从头扫描单页面原件，最多返回 16 MiB，无后段游标或 checkpoint 定位；UI 只有播放/暂停。事件和正文已有分页，不等于全部历史入口都有分页。 |
| checkpoint 限时 | 已补从取得锁起的 10 秒采集预算、UI 与 HTTP job 定向取消、部分材料保存及晚到结果隔离；页面丢失后由页面关闭清理流程安全解锁，runner 不把不完整/不一致采集计入覆盖。写入阶段必须等待耐久结果，磁盘故障不在 10 秒保证内；闸门静默失败仍保持关闭并要求显式停止。 |
| 请求正文完整性 | 已改为独立 `request-body` artifact，关联请求身份、观察字节数、状态及原因；缺失 CDP 文本限时补采，重定向/ID 重用/暂停后旧来源失效。真实 1 MiB 完整、9 MiB 补采并在 8 MiB 截断、凭据/二进制/multipart 排除及有界回读通过；不承诺文件上传或通用个人信息识别。 |
| 采集/索引进程分工 | `Studio` 直接创建 capture/store，启动时 `EvidenceStore.open()` 逐 run 重建索引；仅 runner 使用 Worker。架构规划中的采集/索引重活迁出主线程尚未落地；不能据此直接断言性能已失败。 |
| 人工评审回读 | 已增加有界 GET reviews 与 UI 分页、保存后显示、关闭重开读取；追加格式兼容既有 `reviews.jsonl`，保留完整理由和范围，不改机器结果。新进程回读、固定游标边界、损坏尾部与真实 React/IPC 回归通过。 |
| 示范基线身份 | 下拉候选与实际对照均核验 projectId，真实 UI 已验证同 key 的跨项目示范不会出现。基线选择仍在 React state；`referenceEvidence` 尚未消费为持久基线关系。 |
| 异常恢复 | 已用 OS 启动身份和内核 guard 核验 writer，dead/PID 重用才可回收并保留旧锁；unknown/corrupt 保持原件并在 UI 显示原因。validation 在 worker 前登记，报告与终态为事实来源、目录可重建；新协议核对身份/输入/版本/材料 hash，孤立报告不恢复结论。强杀后 checkpoint 可读、复跑建立新 run、再次启动不重复登记或追加同一 gap。恢复不重新取得旧 lease/worker/人工等待；扫描仍在主进程。 |
| 长测结论范围 | `test/desktop/soak.ts` 约每 10 秒执行点击和请求、每分钟保存 checkpoint；最终断言 checkpoint 数量及重建 ID，P95/内存/gap 仅记录。即使该入口完整通过，也不能直接证明 30 分钟全负载、事件/请求零丢失、内存增长受控或所有性能阈值。 |

较小的界面收尾还包括前进/后退/刷新按钮和 checkpoint 小图。Forge 打包白名单不包含 `skills/`、`docs/`、`examples/`；当前 ZIP 的启动验证不等于这些接续材料已随包交付。版本指纹限定为当前实现收集的 JS/TS/JSON/YAML 等文件与指定锁文件，来源验证首先检查引用是否登记；其他配置/数据格式及业务语义仍需按实际工作流验收。

## 历史修复和验证边界

- 2026-09-23 已将根包、主进程/runner、配置和测试入口迁至 ESM；沙箱 preload 保留单文件 `preload.cjs` 产物。模块规范见 [architecture.md](architecture.md#11-模块格式定调2026-09-23)。类型检查、94/94 单测与 19 进程桌面矩阵通过，报告 `output/desktop-1790101014732/desktop-summary.json`；Forge 开发启动/退出、package 和打包 EXE 的 17 进程恢复专项也通过（`output/desktop-1790101407643`）。同目录前端重构的中间状态未作为整体验收，不把这些专项结果外推到新界面、最终源码或新分发 ZIP，详情见 [验证记录](verification.md#esm-模块迁移2026-09-23)。
- 2026-09-23 修复开发启动 `127.0.0.1:5173` 的 EACCES：本机 Windows TCP 排除范围覆盖该端口，renderer 改由系统动态分配 loopback 端口。类型检查、Forge 开发启动主阶段、真实界面与 HMR 连接通过，报告为 `output/forge-start-1790099842887/test-result.json`；未据此扩大到 profile 重启、长测或发行验收，详见 [验证记录](verification.md#开发启动端口修复2026-09-23)。
- 已按用户要求改为 Vite，不保留 Webpack 构建链。
- Electron 弹窗报错 `Invalid webContents` 已修复：接管 Electron 传入的 guest WebContents，真实弹窗、opener、切页和自关闭均通过。
- 普通 Puppeteer 点击被遮挡窗口节流、rrweb 辅助 iframe 递归、preload 文件名不一致、操作连接与取消竞态已修复并回归。
- reader 在并发追加后补 cursor 导致预算溢出的问题已修复。实现提交前全量单测为 **42/42**，类型检查通过；新增用例覆盖真实追加竞态和长元数据分页。
- 本机 HTTP/CDP 实测可通；未修改防火墙。安装失败观察指向 npm 代理 TLS reset 和执行环境限制，不能归因为 Public 入站规则。
- 迁移时曾在主目录独立安装依赖，类型检查、42/42 测试、构建、当时源码两进程桌面与 EXE 两进程回归均通过。历史 D: ZIP 对应 `f8e6eb8`，包含 reader 修复；迁移前旧 ZIP 为更早产物。报告、大小、摘要与历史路径见 [verification.md](verification.md#主目录迁移后的复验)；旧材料未复制到本轮工作区。
- 两次 20 分钟长测均未通过：第一次约 11 分钟被导航到非合成页面中断；第二次最后保存到 18.19 分钟、18 个 checkpoint，Electron 在完成报告前以退出码 0 退出，启动器因缺少报告返回 1。未生成最终 soak 结果，也未进入第二进程阶段；日志无法确认退出原因。阶段文件中的 running 只是退出前快照，不代表仍有测试在运行。
- 技能独立接续尝试只取得 capabilities/health/state；额外只读查询被自动审批以超出当时授权范围为由拒绝，未继续绕过。来源、附件、指纹和修改/复跑接续尚未完整验收。

## 下一步

用户于 2026-09-23 提前安排的前端重构已完成本批实施、自动化回归和截图视检，详细方案及保留边界见 [frontend-refactor.md](frontend-refactor.md)。可先人工体验 Windows 物理鼠标跨分界拖动与新界面，再继续下列既有收尾工作；本批未自行扩展到这些事项。

1. 使用本批退出诊断，从主目录重新完成 20 分钟长测及重启阶段；此前 18.19 分钟提前退出的具体原因仍未知。本批没有重跑长测，也没有将退出诊断通过写成长测通过。
2. 补长历史续读与 checkpoint 回放定位、示范基线持久关联，再按实际负载决定采集/索引进程拆分。单独测事件/请求完整性和内存边界；100 ms 界面反馈、30 分钟全负载、HTTP P95 300 ms 目标不从短回归外推。
3. 后续发行前按影响复跑桌面并生成 ZIP，核对源码、锁文件、产物和验证证据对应；当前源码的应用目录和打包主阶段已通过，分发 ZIP 仍是历史版本。
4. 完成项目技能从入口和摘要定位来源、修改脚本、复跑的接续验收，并明确技能/说明/独立示例的交付方式。
5. 用户方便配合时，进行真实演示与扫码，生成新业务脚本并逐需求验收。保留来源、版本、范围与未覆盖项，不修改旧业务插件。

签名/自动更新、跨机器 profile 迁移、外部浏览器接管及完整跨域 iframe/Canvas/媒体回放不在当前通过声明中。
