# 当前进度与接续

2026-09-26 06:14 本次完整重构按用户要求暂停。已完成主控真实派发、审查和多轮集成；A 的录制/离线同源 frame 专项、首轮固定资料→授权→真实 worker→双实体显示值核验系统链通过，D/E 基础模块已接入。完整重构未完成：实际新 UI、剩余资源边界、交接导出、安装包、新 Agent 与新架构长测继续待验收。当前实现/测试提交 `85a7eca`；准确目录、任务 ID、待审查提交与 A 未提交 fixture 见 [暂停接续记录](refactor-handoffs/PAUSE-20260926.md)，整体范围见 [重构状态](refactor-status.md)。以下 S0 与旧版本事实保留，不表示新架构已全体验收。

2026-09-26 S0 重构已实现并跑通专项：录制封存保留 live session，续录新建 writer/full snapshot；增加独立导航/显式关闭、当前页试跑与从起点验证、按目标停止；冻结 A/B/C 的类型和文件所有权。rrweb 原型真实验证源属性→Mirror→历史源结构→CSS/XPath，发现并在原型采集时修正 checkbox value 遮罩漏洞（生产接入留 A）。完整范围、失败与实测限制见 [S0 交接](refactor-handoffs/S0.md)、[重构状态](refactor-status.md) 和 [基线](refactor-baseline.md)。本次仅按用户授权新建隔离 worktree，主目录仍为最终集成点；旧 M0–M6 记录以下保留，不代表新重构已全部完成。

更新日期：2026-09-24。当前为已有合成通过记录的开发版本，M0–M5 核心已实现，异常 writer、验收恢复及本次长历史展示缺陷已修复；最新发行包的 30 分钟固定负载、证据重开与恢复矩阵已通过。页面私有内存仍有正向增长，成因未确定；技能接续和真实业务也未整体验收。本文用于接续工作，历史命令和本次审查边界详见 [verification.md](verification.md)。不能把“核心已实现”理解为各阶段全部验收目标完成。

2026-09-24 审查修复已收紧 HTTP 人工交还与评审写入、action 页面代际和目标身份；封存档案重开检查原件哈希与缺失，损坏时保留可读原件、标记 interrupted 和显式缺口；摘要 checkpoint 游标可用于正文续读。前端检查模式以页面实际状态为准，连续点击仍拦截网站动作，存档材料与时间线迟到响应不会串入新选择；验收对照会有界续读长历史。测试模式要求独立绝对 `BES_DATA`，发行包缺少该配置时直接以退出码 2 拒绝。独立订单示例拒绝复用非空输出目录；京东示例 7/7 纯合成测试通过，三次登录点均已声明。`npm.cmd test` **26 文件 / 155 项**、类型检查与构建通过；源码和发行包的 19 进程矩阵分别通过。最新应用包的 30 分钟固定负载完成 **1,800/1,800**、零漏槽并通过新进程证据重开与恢复矩阵；页面私有内存仍有正向增长，真实账号尚未复跑。具体产物与限制见 [本轮验证](verification.md#整体审查修复2026-09-24)。

后续源码对 observer 连接试设 `networkEnabled: false`，补电源/窗口诊断与稳定的 `EMPTY_ITEMS`；打包场景暴露 `workspace.json.tmp` 的 `EPERM` 后，`Studio.save` 改用现有 `atomicJson` 并重打包。最新包先以 `BES_SKIP_UI=1` 完成五分钟短预检 **300/300**，再完成 30 分钟固定负载 **1,800/1,800**、零漏槽、**1,830** 个响应和 **30** 个 checkpoint；新进程核验 **1,917** 个文件、五处强杀恢复和两种退出场景通过。页面私有内存最高 **302.73 MiB**，去除前五分钟后的增长斜率约 **7.54 MiB/分钟**。新轮窗口在约第 **10.33 分钟**后隐藏，不能将低于旧轮的内存数值归因于 observer 设置，也不能称内存稳定。最新包 `BES_TEST` 缺少 `BES_DATA` 的启动守卫复验为退出码 2、不创建正式目录；独立 Chrome 订单示例在正常桌面进程环境 **2/2** 通过。详见[验证记录](verification.md#整体审查修复2026-09-24)。

2026-09-24 按[选型评估](test-runner-assessment.md)将普通单元与模块测试迁移到 Vitest，使用独立配置及 `npm test` 一次性入口，并增设 `test:watch`。原生 Node 子进程、独立示例和真实 Electron 桌面矩阵保留各自入口；Vitest 结果不替代这些验收。实际执行结果与未覆盖范围见 [验证记录](verification.md)。

2026-09-24 又补齐 8 项前端行为测试：存档乱序响应与旧材料隔离、验收示范的项目/run 身份及乱序选择、材料视图的明确空值/截断/定向续读。存档测试先复现 A 的迟到响应覆盖 B 的真实竞态，现用选择身份与请求代际保护历史、材料和事件读取。类型检查、构建及 Vitest **25 文件 / 142 项**通过；桌面回归与限制详见[验证记录](verification.md#前端行为测试与存档切换竞态2026-09-24)。

2026-09-24 已实现人工控制下的一次性验收启动授权：可信 UI 签发 120 秒 grant，HTTP 只查询并在原 validations 路由消费，绑定 run/project/profile/workflow/lease、页面、代码版本和输入；普通控制接口仍保留 human guard。启动全程可按 job 取消，撤销不被队列阻塞，失败事件在返回前发布索引。类型检查、构建、**134/134** 串行单测及 **19 进程**桌面矩阵通过，其中授权专项 **20 项**检查包含真实 UI 按钮和 HTTP 消费。新客户端需重新启动并由用户在“执行 / 验收”点击“允许 Agent 启动一次”。未改京东业务代码或复验真实账号，详见 [验证记录](verification.md#一次性授权-agent-启动验收2026-09-24) 和 [调用方式](api.md#人工控制下授权-agent-启动验收)。

京东参考插件提交前检查的 **4/6** 是修复前历史结果；现已把测试调整到列表派生摘要的真实契约，并覆盖重试交接 ID，当前 **7/7** 纯合成测试通过。此参考版本尚未完成真实主流程验收，详见 [本轮验证](verification.md#整体审查修复2026-09-24) 与 [原检查记录](verification.md#京东参考插件提交前检查2026-09-24)。

此前京东真实验收 run `19c62128-4e78-491f-9819-43b89b5df45a` 的 worker 原始错误为订单年份选项不可见，transport 关闭标记为 worker 本地清理，不是框架断线。插件已调整当年菜单项匹配，真实复跑待确认；profile、identity、addresses 断言也仍失败，详见 [实际运行记录](verification.md#操作连接关闭与原始错误保留2026-09-24)。

后续 run `0435ff23-307f-4ec7-8c6f-c77a1abebf27` 验证了一次性 UI grant 可被 Agent 正常读取、消费并启动 validation；执行在地址阶段失败，历史地址 URL 到达京东首页，导致旧选择器 20 秒超时。示例现在根据个人信息页可见链接导航并识别首页落点；`node --check` 通过，但修改后需新的用户一次性授权才能真实复跑。详见 [本次地址导航诊断](verification.md#一次性授权启动后的京东地址导航失败2026-09-24)。

2026-09-24 后续 run `eb862014-114b-41d8-8d79-fb3d3b2a10f7` 的断线诊断发现另一条确定框架缺陷：worker 异常清理先断开 Puppeteer，主进程会用通用断线错误抢先覆盖原始异常。已修正本地关闭与远端通知的结算优先级，补有界 socket/Gate 关闭记录、原始错误来源和栈；旧 run 未保存原始异常，仍不能追溯认定为网络或某个选择器错误。京东示例同步按阶段立即保存六类数据。类型检查、构建、串行 **124/124** 框架测试、**6/6** 示例合成测试和跳过 UI 专项的 19 进程桌面矩阵通过；默认并发单测首轮 121/123，两项 OS 身份查询失败保留记录。未操作真实账号或读取订单正文，详见 [断线诊断修复](verification.md#操作连接关闭与原始错误保留2026-09-24)。

2026-09-24 新增 `npm run start:agent`：保留 Forge/Vite 开发流程，控制台同时落盘，仓库固定入口 `output/dev/latest.json` 指向每次启动摘要、日志及应用连接/生命周期路径；项目技能补充发现和实例核对规则。另修复人工交接时 Puppeteer target 维护被误判为业务越权、重复交还被拒的问题，补 runner 冲突方法记录；真实运行的具体被拒方法此前未落盘，不能把合成根因直接当作该账号全部异常的归因。112/112 测试、构建、隔离 `start:agent` 冷启动及跳过 UI 专项的 19 进程矩阵通过，详见 [本轮验证](verification.md#agent-开发入口与人工交接修复2026-09-24)。未操作真实账号、重启用户客户端、继续京东业务脚本或重新打包。

2026-09-24 启动修复已提交为 `b280b87`。随后完成并按用户要求修订 [electron-builder + Vite 迁移评估](build-tooling-assessment.md)：直接核对 builder 仓库声明与 26.16.1 tag 的打包实现，建议保留 Vite 8.3.0，以少量项目启动代码衔接 Electron，由 builder 负责分发；保留现有运行架构。社区集成项目仅供参考，撤回优先采用 vite-plugin-electron 的建议。锁文件入包、开发生命周期与打包后运行仍需验证；尚未安装、实施或验证新组合，当前基线仍为 Forge + Vite。

2026-09-24 修复开发启动布局超时的一条已复现路径：此前可信窗口一律拒绝导航，导致 Forge/Vite 自动刷新被拦截；首次模块失败时 React 无法恢复挂载。现仅允许精确可信 UI URL 的刷新，保留其他导航拦截、重新上报边界及原有 20 秒期限，并补启动失败的有界生命周期诊断。类型检查、107/107 测试、构建、Forge 与构建文件两组启动专项通过，包含持续模块失败的正确失败判定；入口 `npm run test:startup`。原始偶发事件未留模块错误，不能证明它与合成故障完全同因；本轮未重新打包或验收长测，详见 [启动修复记录](verification.md#开发启动布局超时修复2026-09-24)。

最新前端重构已按“先文档后实现”完成：shadcn 组件、亮/暗主题、紧凑左右布局和可调整信息区已通过类型检查、102/102 测试、19 进程桌面矩阵及 Forge 开发验证，报告 `output/desktop-1790104067949/desktop-summary.json`。同步修复窗口销毁后的迟到 IPC 异常与被拒导航导致浏览器隐藏的问题；Windows 物理鼠标跨原生视图命中仍待人工体验检查。详细范围见 [前端方案](frontend-refactor.md) 与 [验证记录](verification.md#前端重构与窗口-ipc-修复2026-09-23)。下方较早批次的计数和锁文件说明保留历史口径。

前端交接后已完成统一源码别名：`@/* -> ./src/*`，三份 Vite 配置读取 TS paths，shadcn 五个 aliases 指向 `@/renderer/...`。隔离 CLI 实际生成、类型检查、102/102 单测、19 进程矩阵、Forge 开发/HMR、package 和新包主阶段通过；首轮桌面拖动曾被额外 `buttons=0` 事件打断，同构建未改代码重跑通过，事件来源仍未确认。CLI 直接联网问题也未宣布修复。详情见 [路径方案](import-alias-plan.md) 与 [验证记录](verification.md#统一源码别名2026-09-23)。该次路径迁移时 Vitest 仍为[选型评估](test-runner-assessment.md)，当时没有实施测试框架迁移。

2026-09-23 先在 `main` / `7d011c8` 上静态核查，发现当时本地产物和最新文档记录不对应；用户随后要求重新打包并推进长测。现已完成 `npm run make`，生成新的 Windows 应用与 ZIP，类型检查和 **107/107 测试**通过。长测增强前的新包 **19 进程矩阵通过**，增强后 **1 分钟机制预检及 19 进程矩阵通过**；最终包的两次完整尝试均在既有 UI 拖动问题处失败，随后用 `BES_SKIP_UI=1` 执行 30 分钟长测专项。最终包、源码摘要、各次失败与通过范围见 [本批验证记录](verification.md#本地重新打包与长测推进2026-09-23)，较早静态核查保留为历史记录。

该历史长测已结束：完成 30 分钟，但因 **1,799 / 1,800** 轮而整体失败；受核对点击、请求正文和封存原件通过。另行重开 Electron 后由新 Node 进程核验长档案，**1,919 个文件和 31 个 checkpoint** 的 hash/索引一致。页面私有内存增至 **488.61 MiB**；独立重开诊断不覆盖原失败，也不作为自动第二阶段通过。2026-09-24 的新一轮结果见本文顶部及[验证记录](verification.md#整体审查修复2026-09-24)。

用户随后提供两段京东人工录制，并反馈常用 Chrome 正常、客户端异常。只读核对发现登录过程曾成功进入首页，但首页多项接口返回 HTTP 403，截图为“当前页面异常 / 请刷新或切换账户试试”的风险兜底页面。客户端环境/profile 差异是优先排查方向，具体触发因素未确认，不能据此判断账号被封禁；真实站点兼容性仍未通过。详见 [诊断记录](verification.md#京东人工录制的只读诊断2026-09-23)，该诊断阶段未运行测试或实施修复。

用户随后授权尽量靠近 Chrome，并提供 ChatGPT 内置浏览器正常登录的参考。已实施 `chrome-compatible-v1`：启动时原生缩减 UA、去除应用/Electron 标记、禁用 Blink `AutomationControlled`，新 run 保存浏览器环境配置。保留原生 Client Hints/getter、普通 Puppeteer、profile 隔离与安全边界。独立基线确认 Electron 的导航请求本来不带 UA-CH；本轮不将该原生限制写成已解决。源码类型检查、107/107 单测、构建和跳过 UI 专项的 19 进程矩阵通过，合成兼容专项覆盖首次导航/刷新/iframe/弹窗；完整命令与矩阵结果见 [兼容验证](verification.md#浏览器兼容策略与原生环境对照2026-09-23)。需完全退出旧开发客户端后 `npm.cmd start` 才能试用新启动配置；本轮未重新生成 EXE/ZIP，先前发行产物仍是旧策略。真实京东效果待人工复测。

用户已在新版开发客户端授权 agent 实测。确认策略和实际请求 UA 均生效；受控重新导航一次后，固定新事件区间仍有 **25 次 HTTP 403**，前后 checkpoint 截图相同，真实京东兼容性仍失败。代表接口已完成 TLS 1.3 并收到 HTTP/2 403 空正文；控制台连接关闭另关联到 SSO 请求。确认客户端经系统代理 `127.0.0.1:7897`（verge-mihomo），但不同浏览器的实际代理路径尚未对照，根因仍未隔离。本次只做实时诊断、保存证据及文档更新，已交回 human 控制，保留原录制。详见 [受控实测](verification.md#京东兼容调整后的受控实测2026-09-23)。

进一步按用户要求参考本机 Codex 内嵌浏览器：安装包自标识为 **Owl** 运行时，随包 chrome.dll 为 **153.0.8010.53**，可见持久浏览器会话、原生权限提示与 debugger/CDP 接入。Codex 的网络服务进程也有到 **127.0.0.1:7897** 的实际连接；“经过代理”本身不足以解释两个应用的差异，但尚未按京东请求证明出口相同。本轮只读检查和更新文档，未修改客户端实现；可借鉴范围及尚未执行的最小对照见 [浏览器参考评估](browser-compatibility-reference.md)。

用户又提供旧原型正常登录、加载京东的反馈及启动日志。只读关联当前原型 manifest、配置和进程，确认它使用完整的 **Google Chrome 153.0.8010.53**，Playwright recorder/tracing 在导航前开启，存在 CDP 与页面脚本注入；网络服务同样连接 **127.0.0.1:7897**。因此不能笼统归因于自动化、录制或代理；下一步应隔离本项目特有采集与 Electron 运行时/session 差异，纯 Electron 若仍失败则优先评估标准 Chrome/Edge 执行模式。原型业务成功仍为用户观察，本轮没有重新操作京东、读取 Cookie/profile 或修改旧项目。详见 [原型对照](browser-compatibility-reference.md#旧原型的实际浏览器与采集对照)。

用户已授权执行无采集对照。新增独立 `browser:baseline` 入口，完全跳过 Studio/CDP/rrweb，以相同 Electron 和环境策略启动全新 profile。类型检查、独立构建及 **5 个合成上下文与关闭清理**通过；首次执行沙箱 GPU/页面加载失败保留为失败记录，正常桌面环境重跑通过。真实启动后用户指出窗口不可见，已修正启动器的 `windowsHide` 选项、重开并用 Computer Use 确认京东原生窗口可见且置前。用户随后明确反馈 **首页和订单均正常**，因此 Electron 在此条件下可用；正继续准备全新 profile 的普通录制对照，区分采集与旧会话因素。现有客户端和旧原型未被接管；命令、报告及对照限制见 [验证记录](verification.md#独立无采集-electron-对照2026-09-23)。

普通录制对照也已通过独立 `--recording` 构建启动，PID **17540**，新 run `c797689d-8a86-4f46-b812-75ee472ebd94`，项目名 **JD fresh-profile recording comparison**。页面由 human 控制，已产生采集数据，但 capture=degraded 的原因尚未核对。定位窗口时用户按 Escape 停止 Computer Use，已停止桌面操作；这一组首页/订单结果仍待用户反馈，不能据无采集成功单独归因 rrweb 或旧 profile。

用户随后确认该普通录制客户端也正常登录、查看订单，并再次交出控制。已只读核验订单截图，保存 **complete / consistent** 的 `cp-000000007518`；固定区间已记录 **828 个 200、13 个 304，未见 403**，两页均有采集就绪和 rrweb 快照。这证明本次新 profile 的正常采集组合可用，排查重点转为旧 profile/会话和当时运行状态，尚不能断言旧 profile 损坏。另确认 degraded 来自采集队列预算触顶，GET run 的旧 manifest 缓存会隐藏健康原因；两项采集/诊断问题尚待独立修复。已交回 human / leaseEpoch=6，保留录制。详情见 [新环境实测](verification.md#全新-profile-的普通录制京东访问确认2026-09-23)。

用户再次反馈异常后，已只读确认当前 PID **35448** 是 **`npm run start` 的 Forge 开发实例**，使用默认 `BrowserEvidenceStudio-dev` 数据根；新 run `0c8002d1-2f3e-450b-a68c-f0cc104bec18` 沿用前三次异常录制的同一 profile `1a88eaa6-2ebf-452e-95b1-bea37c287413`，兼容策略已经生效。此前成功的独立普通录制使用另一数据根和全新 profile。此次尚未独立核验页面/响应，不将用户报告的复发判为 Forge 或 profile 的单因果证明；下一步仍是在同一开发客户端对照新旧 profile。详见 [启动核对](verification.md#再次异常后的启动方式核对2026-09-23)。

用户完成新 profile 的主流程示范后，已检查封存 run `efc0c4e5-e9b0-470a-8f0b-0ec5636f1c4f`：14 个 checkpoint 完整一致，17,848 条落盘记录、引用及文件 hash 通过，但存在 **5,939 个采集任务丢弃**，不能称为无缺口连续录制。主线材料足以准备业务 agent 初版；末页、非空回收站、详情变体与身份字段约定仍待补充。按用户要求仅检查和更新文档，未开始业务编写。详见 [存档评估](recording-review-efc0c4e5.md)。

最少提示的 **gpt-6-luna / max** 新任务已启动；首次接续观察发现它理解本机 HTTP，但因技能只指向“客户端展示的连接文件”、交接未给绝对路径，转去找窗口并在工具/权限失败后询问用户路径，尚未进入应用证据读取。原任务独立 health 检查为 ready；新任务同时读了含已有审查摘要的工程进度/验证文档，本轮不作为严格盲测。仅记录发现，未向它追加解题提示或修改技能，详见 [交接观察](verification.md#luna-首次任务交接的连接观察2026-09-23)。

## 工作目录与提交

- 固定开发目录：`D:\workspace\browser-evidence-studio`，分支 `main`。后续代码、测试和文档均在这里维护。
- 实现提交：`f8e6eb8`，包含 Vite 客户端、采集与证据、HTTP、执行/验收、示例、测试及最后的 reader 预算修复；从 `codex/initial-client` 快进合并，无冲突。
- 旧 worktree：`C:\Users\Administrator\.codex\worktrees\c22c\browser-evidence-studio`。停止在此开发，保留迁移前本地证据；原长测进程已退出，不再在旧目录启动测试。Git 合并只搬运跟踪文件，不复制录制、Cookie、profile 或其他运行数据。
- 历史文档提交为 `1c3c3b1`，审查与通用 Node 环境说明为 `14f64fb`；请求正文/checkpoint/评审为 `10f8f5d`，writer 锁为 `7a6ef3f`，验收持久化与恢复界面为 `9946a4b`。后续退出诊断与矩阵验证见本批提交记录。不要将开发验证结果视为正式签名发布或真实业务验收。

## 阶段进度

| 阶段 | 已实现与已验证 | 仍需完成或确认 |
| --- | --- | --- |
| M0 桌面技术闭环 | Electron Forge + Vite + TypeScript + React；WebContentsView、明确 targetId、独立采集/操作连接、原生蒙版；开发启动、真实 Electron 和打包 EXE 已运行 | 每次最终源码改动后保持产物与验证版本一致 |
| M1 证据与恢复 | 追加原件、JSONL/可重建索引、hash、尾部损坏恢复；请求/响应正文独立状态与预算；已补 OS writer 身份、独占回收、原锁保留、原子发布和重复重开幂等 | 采集/索引进程分工；长负载及数据损失结论仅限实际测试边界，非 NTFS/其他桌面平台未验收 |
| M2 录制工作台 | 项目/profile、录制、checkpoint 截图/DOM、元素检查、时间线、历史证据和顶层 DOM 回放；已补 10 秒采集期限、定向取消、部分材料保存及晚到结果隔离 | 长历史续读与回放定位、基础导航与缩略图仍有缺口；完整跨域 iframe/Canvas/媒体回放不作承诺 |
| M3 人机协作 | 可撤销操作连接、控制权/代际、人工完成检查、取消与超时；两处人工窗口、合成登录态跨进程复用和 profile 隔离；等待人工时强杀后保留材料并转中断 | 真实扫码、真实会话失效及真人恢复体验；不承诺恢复旧执行栈和人工等待 |
| M4 本机 HTTP | loopback/token/Host/Origin、异步 job、幂等、身份验证、有界证据读取；真实 8 个完成 job、6 次拒绝、幂等点击仅一次 | 长负载下 HTTP 性能目标与来源级技能接续 |
| M5 普通脚本与验收 | 普通 Puppeteer worker、薄 manifest、字段/覆盖/来源/版本验收；评审回读与同项目示范过滤；已补启动登记、报告/终态提交、证据校验重建和中断后新 run 重试 | 示范基线持久关联，以及真实业务需求与新脚本的端到端验收；独立 Edge 五变体仍为历史通过记录 |
| M6 交付 | 项目技能、统一合成夹具、默认 30 分钟固定负载入口；实例/PID/清理退出诊断；最新应用包通过 30 分钟固定负载、证据重开及恢复矩阵，同版 ZIP 已生成 | 页面私有内存持续增长的成因、ZIP 独立启动与 UI 拖动问题、技能独立接续与真实业务验收 |

实际源码主要位于 `src/main/services/studio.ts`、`src/main/services/dispatch.ts`、`src/capture/`、`src/evidence/`、`src/runner/` 和 `src/renderer/app.tsx`；架构/实施文档中的部分细分目录仍属于职责规划。

## 较早批次代码与工作区核查

初次静态审查基线为 `main` / `1c3c3b1`（实现 `f8e6eb8`），审查时未重跑测试；其文档结果已提交为 `14f64fb`。用户随后授权实施下一步，本轮恢复依赖并补齐以下数据、控制与评审缺口。实际运行记录见 [verification.md](verification.md)，未访问真实账号材料。

该较早批次终端实际 Node/npm 为 **24.21.0 / 11.19.0**，重新安装依赖、构建客户端并产生合成证据，当时没有重新生成发行 ZIP。当前批已重新 make，见本文顶部。项目仅约束 Node/npm 版本，安装方式和版本管理器由开发者选择；历史产物和旧报告不改写为当前通过。

上一批 `10f8f5d` 的 **65/65 单测**、Vite 构建与两进程 Electron 报告为 `output/desktop-1790092245546/desktop-summary.json`。当前批最终类型检查、**94/94 测试**、Vite 构建和 **19 进程 Electron 回归**均通过，最终报告为 `output/desktop-1790095701394/desktop-summary.json`。覆盖原有主场景/profile 重启、五个强杀位置的两次重开与新 run 复跑，以及目录保存期间保持 finalizing、已有 checkpoint 下连续关窗/app.quit 的完整清理。详情见 [verification.md](verification.md)。

初次审查项及本轮收尾后的边界：

| 项目 | 源码依据与当前边界 |
| --- | --- |
| 长历史浏览与回放 | 前端重构移除了列表前 15 个 run 的额外裁剪；`state()` 返回全部 run，`runs` 列表接口只返回前 100 条且无续页。checkpoint/gap 的 UI 续页仍未补齐。`Studio.history()` 首批 checkpoint 最多 100 条且受 32 KiB 限制。`Studio.replay()` 从头扫描单页面原件，最多返回 16 MiB，无后段游标或 checkpoint 定位；UI 只有播放/暂停。事件和正文已有分页，不等于全部历史入口都有分页。 |
| checkpoint 限时 | 已补从取得锁起的 10 秒采集预算、UI 与 HTTP job 定向取消、部分材料保存及晚到结果隔离；页面丢失后由页面关闭清理流程安全解锁，runner 不把不完整/不一致采集计入覆盖。写入阶段必须等待耐久结果，磁盘故障不在 10 秒保证内；闸门静默失败仍保持关闭并要求显式停止。 |
| 请求正文完整性 | 已改为独立 `request-body` artifact，关联请求身份、观察字节数、状态及原因；缺失 CDP 文本限时补采，重定向/ID 重用/暂停后旧来源失效。真实 1 MiB 完整、9 MiB 补采并在 8 MiB 截断、凭据/二进制/multipart 排除及有界回读通过；不承诺文件上传或通用个人信息识别。 |
| 采集/索引进程分工 | `Studio` 直接创建 capture/store，启动时 `EvidenceStore.open()` 逐 run 重建索引；仅 runner 使用 Worker。架构规划中的采集/索引重活迁出主线程尚未落地；不能据此直接断言性能已失败。 |
| 人工评审回读 | 已增加有界 GET reviews 与 UI 分页、保存后显示、关闭重开读取；追加格式兼容既有 `reviews.jsonl`，保留完整理由和范围，不改机器结果。新进程回读、固定游标边界、损坏尾部与真实 React/IPC 回归通过。 |
| 示范基线身份 | 下拉候选与实际对照均核验 projectId，真实 UI 已验证同 key 的跨项目示范不会出现。基线选择仍在 React state；`referenceEvidence` 尚未消费为持久基线关系。 |
| 异常恢复 | 已用 OS 启动身份和内核 guard 核验 writer，dead/PID 重用才可回收并保留旧锁；unknown/corrupt 保持原件并在 UI 显示原因。validation 在 worker 前登记，报告与终态为事实来源、目录可重建；新协议核对身份/输入/版本/材料 hash，孤立报告不恢复结论。强杀后 checkpoint 可读、复跑建立新 run、再次启动不重复登记或追加同一 gap。恢复不重新取得旧 lease/worker/人工等待；扫描仍在主进程。 |
| 长测结论范围 | 旧入口只核对 checkpoint 数量/重建 ID。2026-09-23 已改为每秒唯一点击与 64 KiB JSON、每分钟 1 MiB JSON/checkpoint，断言动作/请求集合、正文 hash、原件完整性、索引和 gap；同时测量 HTTP、摘要、checkpoint 和内存。具体通过范围见本批验证记录；已提交原件 hash 不证明全部 DOM/rrweb 更新都已采到，有限时长和保护上限不证明内存无泄漏。 |

较小的界面收尾还包括前进/后退/刷新按钮和 checkpoint 小图。Forge 打包白名单不包含 `skills/`、`docs/`、`examples/`；当前 ZIP 的启动验证不等于这些接续材料已随包交付。版本指纹限定为当前实现收集的 JS/TS/JSON/YAML 等文件与指定锁文件，来源验证首先检查引用是否登记；其他配置/数据格式及业务语义仍需按实际工作流验收。

## 历史修复和验证边界

- 2026-09-23 已将根包、主进程/runner、配置和测试入口迁至 ESM；沙箱 preload 保留单文件 `preload.cjs` 产物。模块规范见 [architecture.md](architecture.md#11-模块格式定调2026-09-23)。类型检查、94/94 单测与 19 进程桌面矩阵通过，报告 `output/desktop-1790101014732/desktop-summary.json`；Forge 开发启动/退出、package 和打包 EXE 的 17 进程恢复专项也通过（`output/desktop-1790101407643`）。同目录前端重构的中间状态未作为整体验收，不把这些专项结果外推到新界面、最终源码或新分发 ZIP，详情见 [验证记录](verification.md#esm-模块迁移2026-09-23)。
- 2026-09-23 修复开发启动 `127.0.0.1:5173` 的 EACCES：本机 Windows TCP 排除范围覆盖该端口，renderer 改由系统动态分配 loopback 端口。类型检查、Forge 开发启动主阶段、真实界面与 HMR 连接通过，报告为 `output/forge-start-1790099842887/test-result.json`；未据此扩大到 profile 重启、长测或发行验收，详见 [验证记录](verification.md#开发启动端口修复2026-09-23)。
- 已按用户要求改为 Vite，不保留 Webpack 构建链。
- Electron 弹窗报错 `Invalid webContents` 已修复：接管 Electron 传入的 guest WebContents，真实弹窗、opener、切页和自关闭均通过。
- 普通 Puppeteer 点击被遮挡窗口节流、rrweb 辅助 iframe 递归、preload 文件名不一致、操作连接与取消竞态已修复并回归。
- reader 在并发追加后补 cursor 导致预算溢出的问题已修复。实现提交前全量单测为 **42/42**，类型检查通过；新增用例覆盖真实追加竞态和长元数据分页。
- 本机 HTTP/CDP 实测可通；未修改防火墙。安装失败观察指向 npm 代理 TLS reset 和执行环境限制，不能归因为 Public 入站规则。
- 迁移时曾在主目录独立安装依赖，类型检查、42/42 测试、构建、当时源码两进程桌面与 EXE 两进程回归均通过。历史 D: ZIP 对应 `f8e6eb8`，包含 reader 修复；迁移前旧 ZIP 为更早产物。报告、大小、摘要与历史路径见 [verification.md](verification.md#主目录迁移后的复验)；旧材料未复制到本轮工作区。
- 两次 20 分钟长测均未通过：第一次约 11 分钟被导航到非合成页面中断；第二次最后保存到 18.19 分钟、18 个 checkpoint，Electron 在完成报告前以退出码 0 退出，启动器因缺少报告返回 1。未生成最终 soak 结果，也未进入第二进程阶段；日志无法确认退出原因。阶段文件中的 running 只是退出前快照，不代表仍有测试在运行。
- 技能独立接续尝试只取得 capabilities/health/state；额外只读查询被自动审批以超出当时授权范围为由拒绝，未继续绕过。来源、附件、指纹和修改/复跑接续尚未完整验收。

## 下一步

前端重构方案及边界见 [frontend-refactor.md](frontend-refactor.md)。用户随后授权先重新打包、再推进以下第 1 项；本批长测增强和验证属于该范围。

1. 定位长负载页面私有内存增长和早一版第 20 分钟后部分点击约 940 ms 的原因。2026-09-24 最新包的 30 分钟固定负载完成 **1,800/1,800** 轮、零漏槽，checkpoint P95 **278.03 ms**、HTTP 提交 P95 **5.39 ms**、摘要 P95 **20.26 ms**；页面私有内存最高 **302.73 MiB**，去除前五分钟后的斜率约 **7.54 MiB/分钟**，不能称为内存稳定。窗口状态与早一版不同，无法把内存下降归因于 observer 设置。旧包 **1,799/1,800** 的失败和更早 18.19 分钟提前退出保留为历史问题；新轮未证明二者根因。
2. 补 checkpoint 回放定位、示范基线持久关联，再按实际负载决定采集/索引进程拆分。验收对照的长历史有界续读已修复，其他历史入口仍需按实际范围核对。单独测事件/请求完整性和内存边界；100 ms 界面反馈不从本次长测外推。
3. 最新 Windows 应用和 ZIP 已生成；应用包完成五分钟短预检及 30 分钟固定负载，ZIP 尚未做独立安装/解压启动验收。既有 UI 拖动异常仍需独立核对，`BES_SKIP_UI=1` 的长测不覆盖它。
4. 完成项目技能从入口和摘要定位来源、修改脚本、复跑的接续验收，并明确技能/说明/独立示例的交付方式。
5. 用户方便配合时，进行真实演示与扫码，生成新业务脚本并逐需求验收。保留来源、版本、范围与未覆盖项，不修改旧业务插件。

签名/自动更新、跨机器 profile 迁移、外部浏览器接管及完整跨域 iframe/Canvas/媒体回放不在当前通过声明中。
