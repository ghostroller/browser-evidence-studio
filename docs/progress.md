# 当前进度与接续

更新日期：2026-09-22。当前为已有合成通过记录的开发版本，M0–M5 核心已实现，但仍有功能与恢复收尾；M6 尚未整体验收。本文用于接续工作，历史命令和本次审查边界详见 [verification.md](verification.md)。不能把“核心已实现”理解为各阶段全部验收目标完成。

## 工作目录与提交

- 固定开发目录：`D:\workspace\browser-evidence-studio`，分支 `main`。后续代码、测试和文档均在这里维护。
- 实现提交：`f8e6eb8`，包含 Vite 客户端、采集与证据、HTTP、执行/验收、示例、测试及最后的 reader 预算修复；从 `codex/initial-client` 快进合并，无冲突。
- 旧 worktree：`C:\Users\Administrator\.codex\worktrees\c22c\browser-evidence-studio`。停止在此开发，保留迁移前本地证据；原长测进程已退出，不再在旧目录启动测试。Git 合并只搬运跟踪文件，不复制录制、Cookie、profile 或其他运行数据。
- 历史文档提交为 `1c3c3b1`，审查与通用 Node 环境说明提交为 `14f64fb`；本轮在其上继续实现。不要将开发验证结果视为正式签名发布或真实业务验收。

## 阶段进度

| 阶段 | 已实现与已验证 | 仍需完成或确认 |
| --- | --- | --- |
| M0 桌面技术闭环 | Electron Forge + Vite + TypeScript + React；WebContentsView、明确 targetId、独立采集/操作连接、原生蒙版；开发启动、真实 Electron 和打包 EXE 已运行 | 每次最终源码改动后保持产物与验证版本一致 |
| M1 证据与恢复 | 追加原件、JSONL/可重建索引、hash、尾部损坏恢复；请求/响应正文独立状态与预算，正文补采、凭据排除、1 MiB 完整/9 MiB 截断通过 | 异常锁恢复、采集/索引进程分工仍需收尾；长负载及数据损失结论仅限实际测试边界 |
| M2 录制工作台 | 项目/profile、录制、checkpoint 截图/DOM、元素检查、时间线、历史证据和顶层 DOM 回放；已补 10 秒采集期限、定向取消、部分材料保存及晚到结果隔离 | 长历史续读与回放定位、基础导航与缩略图仍有缺口；完整跨域 iframe/Canvas/媒体回放不作承诺 |
| M3 人机协作 | 可撤销操作连接、控制权/代际、人工完成检查、取消与超时；两处人工窗口、合成登录态跨进程复用和 profile 隔离通过 | 异常锁/PID 重用恢复、真实扫码、真实会话失效及真人恢复体验 |
| M4 本机 HTTP | loopback/token/Host/Origin、异步 job、幂等、身份验证、有界证据读取；真实 8 个完成 job、6 次拒绝、幂等点击仅一次 | 长负载下 HTTP 性能目标与来源级技能接续 |
| M5 普通脚本与验收 | 普通 Puppeteer worker、薄 manifest、字段/覆盖/来源/版本验收；空闲验收/封存后重跑；已补人工评审分页回读、重开显示与同项目示范过滤，机器结果保持独立 | 示范基线持久关联、进行中验收崩溃恢复，以及真实业务需求与新脚本的端到端验收；独立 Edge 五变体仍为历史通过记录 |
| M6 交付 | 项目技能、统一合成夹具、20 分钟测试入口、Windows ZIP；技能结构检查、打包 EXE 两进程回归通过 | 长测提前退出排查与完整复验、技能独立接续、真实业务验收 |

实际源码主要位于 `src/main/services/studio.ts`、`src/main/services/dispatch.ts`、`src/capture/`、`src/evidence/`、`src/runner/` 和 `src/renderer/app.tsx`；架构/实施文档中的部分细分目录仍属于职责规划。

## 本次代码与工作区核查

初次静态审查基线为 `main` / `1c3c3b1`（实现 `f8e6eb8`），审查时未重跑测试；其文档结果已提交为 `14f64fb`。用户随后授权实施下一步，本轮恢复依赖并补齐以下数据、控制与评审缺口。实际运行记录见 [verification.md](verification.md)，未访问真实账号材料。

本轮终端实际 Node/npm 为 **24.21.0 / 11.19.0**；已重新安装依赖、构建客户端，并产生新的合成测试证据。项目仅约束 Node/npm 版本，安装方式和版本管理器由开发者选择。锁文件内容未改；没有重新生成发行 ZIP，历史产物和旧报告不改写为本轮通过。

本轮最终类型检查、**65/65 单测**与 Vite 构建通过；两进程 Electron 回归通过，报告 `output/desktop-1790092245546/desktop-summary.json`。除新增正文与评审界面场景，还验证 checkpoint 实际 UI 取消、页面关闭后的控制恢复及不完整采集不能满足脚本验收。

初次审查项及本轮收尾后的边界：

| 项目 | 源码依据与当前边界 |
| --- | --- |
| 长历史浏览与回放 | `renderer/app.tsx` 只列前 15 个 run，checkpoint/gap 面板没有续页；`Studio.history()` 首批 checkpoint 最多 100 条且受 32 KiB 限制。`Studio.replay()` 从头扫描单页面原件，最多返回 16 MiB，无后段游标或 checkpoint 定位；UI 只有播放/暂停。事件和正文已有分页，不等于全部历史入口都有分页。 |
| checkpoint 限时 | 已补从取得锁起的 10 秒采集预算、UI 与 HTTP job 定向取消、部分材料保存及晚到结果隔离；页面丢失后由页面关闭清理流程安全解锁，runner 不把不完整/不一致采集计入覆盖。写入阶段必须等待耐久结果，磁盘故障不在 10 秒保证内；闸门静默失败仍保持关闭并要求显式停止。 |
| 请求正文完整性 | 已改为独立 `request-body` artifact，关联请求身份、观察字节数、状态及原因；缺失 CDP 文本限时补采，重定向/ID 重用/暂停后旧来源失效。真实 1 MiB 完整、9 MiB 补采并在 8 MiB 截断、凭据/二进制/multipart 排除及有界回读通过；不承诺文件上传或通用个人信息识别。 |
| 采集/索引进程分工 | `Studio` 直接创建 capture/store，启动时 `EvidenceStore.open()` 逐 run 重建索引；仅 runner 使用 Worker。架构规划中的采集/索引重活迁出主线程尚未落地；不能据此直接断言性能已失败。 |
| 人工评审回读 | 已增加有界 GET reviews 与 UI 分页、保存后显示、关闭重开读取；追加格式兼容既有 `reviews.jsonl`，保留完整理由和范围，不改机器结果。新进程回读、固定游标边界、损坏尾部与真实 React/IPC 回归通过。 |
| 示范基线身份 | 下拉候选与实际对照均核验 projectId，真实 UI 已验证同 key 的跨项目示范不会出现。基线选择仍在 React state；`referenceEvidence` 尚未消费为持久基线关系。 |
| 异常恢复 | writer 锁对其他存活 PID 没有核对启动身份，锁 JSON 损坏时直接拒绝，UI 无显式所有权恢复入口。validation 登记在运行开始时只存在内存，结束才保存；启动不从 run 原件重建验收记录。普通失活 writer 恢复、profile 跨进程通过不覆盖这些场景。 |
| 长测结论范围 | `test/desktop/soak.ts` 约每 10 秒执行点击和请求、每分钟保存 checkpoint；最终断言 checkpoint 数量及重建 ID，P95/内存/gap 仅记录。即使该入口完整通过，也不能直接证明 30 分钟全负载、事件/请求零丢失、内存增长受控或所有性能阈值。 |

较小的界面收尾还包括前进/后退/刷新按钮和 checkpoint 小图。Forge 打包白名单不包含 `skills/`、`docs/`、`examples/`；当前 ZIP 的启动验证不等于这些接续材料已随包交付。版本指纹限定为当前实现收集的 JS/TS/JSON/YAML 等文件与指定锁文件，来源验证首先检查引用是否登记；其他配置/数据格式及业务语义仍需按实际工作流验收。

## 历史修复和验证边界

- 已按用户要求改为 Vite，不保留 Webpack 构建链。
- Electron 弹窗报错 `Invalid webContents` 已修复：接管 Electron 传入的 guest WebContents，真实弹窗、opener、切页和自关闭均通过。
- 普通 Puppeteer 点击被遮挡窗口节流、rrweb 辅助 iframe 递归、preload 文件名不一致、操作连接与取消竞态已修复并回归。
- reader 在并发追加后补 cursor 导致预算溢出的问题已修复。实现提交前全量单测为 **42/42**，类型检查通过；新增用例覆盖真实追加竞态和长元数据分页。
- 本机 HTTP/CDP 实测可通；未修改防火墙。安装失败观察指向 npm 代理 TLS reset 和执行环境限制，不能归因为 Public 入站规则。
- 迁移时曾在主目录独立安装依赖，类型检查、42/42 测试、构建、当时源码两进程桌面与 EXE 两进程回归均通过。历史 D: ZIP 对应 `f8e6eb8`，包含 reader 修复；迁移前旧 ZIP 为更早产物。报告、大小、摘要与历史路径见 [verification.md](verification.md#主目录迁移后的复验)；旧材料未复制到本轮工作区。
- 两次 20 分钟长测均未通过：第一次约 11 分钟被导航到非合成页面中断；第二次最后保存到 18.19 分钟、18 个 checkpoint，Electron 在完成报告前以退出码 0 退出，启动器因缺少报告返回 1。未生成最终 soak 结果，也未进入第二进程阶段；日志无法确认退出原因。阶段文件中的 running 只是退出前快照，不代表仍有测试在运行。
- 技能独立接续尝试只取得 capabilities/health/state；额外只读查询被自动审批以超出当时授权范围为由拒绝，未继续绕过。来源、附件、指纹和修改/复跑接续尚未完整验收。

## 下一步

1. 已恢复 Node 24.21.0 / npm 11.19.0 下的依赖与基线，完成请求正文状态、checkpoint 限时取消、评审回读及示范项目隔离这一批收尾；继续保持源码、锁文件和验证证据对应。
2. 下一批处理异常 writer 锁/进行中验收恢复、长历史续读与回放定位、示范基线持久关联，再按实际负载决定采集/索引进程拆分。补齐后按影响复跑桌面，并重新打包验收。
3. 排查 20 分钟长测在完成报告前退出的原因，从主目录完成长测及重启阶段。单独补测负载、事件/请求完整性和内存边界；100 ms 界面反馈、30 分钟全负载、HTTP P95 300 ms 目标不从短回归外推。
4. 完成项目技能从入口和摘要定位来源、修改脚本、复跑的接续验收，并明确技能/说明/独立示例的交付方式。
5. 用户方便配合时，进行真实演示与扫码，生成新业务脚本并逐需求验收。保留来源、版本、范围与未覆盖项，不修改旧业务插件。

签名/自动更新、跨机器 profile 迁移、外部浏览器接管及完整跨域 iframe/Canvas/媒体回放不在当前通过声明中。
